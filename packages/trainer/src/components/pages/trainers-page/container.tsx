import React from 'react';
import Layout from '../../core/layout';
import { Box } from '@material-ui/core';

export const TrainersPageContainer: React.FC = () => {
  return (
    <Layout>
      <Box bgcolor="primary.light">TLDR</Box>
    </Layout>
  );
};

export default TrainersPageContainer;
